services:
  kb-mcp-http:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-research-kb:latest
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      KB_STATEFUL_SESSIONS: "true"
      KB_ENABLE_WRITES: "false"
    restart: unless-stopped
